const { types } = require('../lib/utils/types');

const {
	Damage,
	Health,
} = types;

module.exports = {
	[`Dancer's Touch`]: Damage,
	'[Dance] Dance of Hope': Health,
	'Divine Blessing': Health,
};
